#!/usr/bin/env node
// TEMPORARY: Claude Code sessions started before the hooks were switched to src/main.ts still run
// this path. Delete this file once those sessions have been restarted.
import "./main";
